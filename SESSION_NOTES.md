# CFRD Support Bot v2 - Заметки для сессий

## Текущий статус (2025-12-13)

**Бот работает!** Тестовая версия запущена с Pyrofork.

**GitHub:** https://github.com/dscfrd/cfrd-support-bot-v2

## Структура проекта

```
/root/cfrd-support-bot-v2/
├── bot.py                      # Основной бот (~4000 строк)
├── config.py                   # Конфигурация
├── database.py                 # Функции БД (~490 строк)
├── venv/                       # Virtualenv с Pyrofork 2.3.69
├── handlers/                   # Пустая (для рефакторинга)
├── bot.log                     # Логи
├── business_account_test.session
└── clients_test.db
```

## Конфигурация (тестовая)

- Телефон: +79851730392
- Группа: -1003317645437 (New_client)
- Сессия: business_account_test
- БД: clients_test.db

## Боевой бот (НЕ ТРОГАТЬ!)

- Путь: /bot/support-cfrd-bot.py
- Телефон: +79859482949
- Группа: -1002675883945

---

## Notion интеграция

**API ключ:** `[СЕКРЕТ - см. .env]`
**Database ID:** `e670d16d6abd49f4bb388727df4291e7`

---

## ЗАДАЧИ ИЗ NOTION

### Статистика
- Нужны правки: 14
- Готово: 19

---

### КРИТИЧНЫЕ (0 осталось) ✅

~~1. **Получение пересланного файла** - ✅ ГОТОВО~~

~~2. **Отправить видео** - ✅ ГОТОВО~~

~~3. **Оповещение об удаленном сообщении** - ✅ ГОТОВО~~

~~4. **Оповещение об отредактированном сообщении** - ✅ ГОТОВО~~

---

### ЖЕЛАТЕЛЬНЫЕ (2 задачи осталось)

1. **Добавить название компании в название треда**
   - Команда: нужна новая
   - Формат: [№ треда: Имя клиента | Название компании]
   - Убрать никнейм клиента из названия

2. **Приветственное сообщение для клиента**
   - Когда клиент открывает чат первый раз

~~3. **Оповещение о поставленной реакции** - ✅ ГОТОВО~~

---

### УДОБСТВО (1 осталось)

~~1. **Отредактировать сообщение** - не реализовано (Telegram не позволяет редактировать чужие сообщения)~~

~~2. **Удалить сообщение** - ✅ ГОТОВО (команда /del)~~

3. **Отчет об отправке сообщения**
   - Реакция на сообщении вместо сообщения от бота

---

### УПРАВЛЕНИЕ МЕНЕДЖЕРАМИ (новые задачи)

1. **/reassign @старый @новый** - Переназначить все треды
   - Когда менеджер заболел/уволился
   - Все треды старого менеджера переходят к новому

2. **/unassign @менеджер** - Снять ответственность
   - Убрать менеджера со всех его тредов
   - Треды становятся "без ответственного"

3. **/remove_manager @менеджер** - Удалить менеджера
   - Удалить из системы при увольнении
   - Снять со всех тредов + удалить из БД

---

### НЕКРИТИЧНЫЕ (9 задач)

1. **/threads** - Просмотр списка активных тредов
   - Отображать треды за 2 недели
   - Добавить группы и ботов (788, 1327, 1308, бот 513)

2. **Убрать возможность отвечать по id клиента**
   - Только id треда для отправки

3. **/wtt** - Просмотр информации о треде
   - Добавить дату первого и последнего сообщения
   - Баг: "первое обращение и последнее обращение неизвестно"

4. **Пересылка сообщения**
   - Менеджер может переслать сообщение клиенту

5. **Поставить реакцию на сообщение**

6. **/files** - Просмотр списка файлов в хранилище
   - Баг: если передумал отправлять файл - ошибка

7. **/ok {ID треда}** - Отключить уведомление
   - Баг: название треда не сбрасывается правильно

8. **/myinfo** - Просмотр своей информации
   - Баг: "too many values to unpack (expected 8)"

9. **Убрать лишние функции**
   - /find, /group_info, /myinfo, ID клиента

---

### ГОТОВО (15 задач)

1. /card {ID_треда} - Отправка карточки клиенту
2. /set_id - Назначение ID заказчику (латиница)
3. /set_id - Назначение ID заказчику (кириллица)
4. /set_id - Назначение ID заказчику (кириллица + цифры)
5. /set_id - Назначение ID заказчику (цифры)
6. /onduty @username {ID_треда} - Назначение ответственного
7. /auth (повторно) - Повторная регистрация менеджера
8. /help - Отображение всех команд
9. /duties - Просмотр списка ответственных
10. Получение текстового сообщения и нескольких файлов
11. Отправить файлы клиенту
12. Получение пересланного видео
13. Получение пересланного изображения
14. Исправлены цитаты (экранирование markdown)
15. Информация о reply (на какое сообщение клиент ответил)

---

## Изменения сессии 2025-12-12

### Форматирование сообщений от клиента к менеджеру

**Обычное сообщение:**
```
**Имя Фамилия @username:**

Текст сообщения
+ 1 видео
```

**Пересланное от пользователя:**
```
**Имя @user** переслал от **Вика @vika**:

Текст сообщения
+ 4 фото
```

**Пересланное из канала:**
```
**Имя @user** переслал из **Название канала @channel**:

Текст сообщения
+ 4 фото
```

**Ответ на сообщение (reply):**
```
**Имя @username:**
↩️ В ответ на: _текст сообщения на которое ответили_

Текст ответа
```

### Форматирование сообщений от менеджера к клиенту

**Подпись менеджера моноширинным:**
```
Текст сообщения
—
`🔵 Иван Петров, менеджер`
```

### Технические изменения

1. **escape_markdown()** - функция экранирования символов `*`, `_`, `` ` ``, `[`, `]` для предотвращения ломания форматирования

2. **reply_info** - отображение информации о сообщении, на которое клиент ответил

3. **Моноширинная подпись** - подпись менеджера в сообщениях клиенту отображается моноширинным шрифтом

---

## Команды для работы

```bash
# Перейти в директорию
cd /root/cfrd-support-bot-v2

# Запустить бота (ЧЕРЕЗ VENV!)
nohup ./venv/bin/python bot.py >> bot.log 2>&1 &

# Проверить логи
tail -f bot.log

# Остановить бота
pkill -f "venv/bin/python bot.py"

# Проверить процессы
ps aux | grep python | grep -v grep
```

---

## Изменения сессии 2025-12-13

### Virtualenv и Pyrofork

- Создан virtualenv в папке `venv/`
- Установлен **Pyrofork 2.3.69** (форк Pyrogram с поддержкой quote)
- Боевой бот использует системный Pyrogram 2.0.106 - **не конфликтует**
- **ВАЖНО:** Запускать бота через `./venv/bin/python bot.py`

### Quote (цитаты) - РАБОТАЕТ!

Теперь при ответе с выделенным текстом показывается цитата:
```
**Имя @username:**
📝 Цитата: _выделенный текст_

Текст ответа
```

### Reply на цитаты (NEW!)

**Добавлена таблица маппинга сообщений** - хранит связь между ID сообщений клиента и ID сообщений в группе.

Теперь при цитате/ответе клиента:
1. Бот ищет соответствующее сообщение в группе
2. Делает reply на него (не просто в топик)
3. Менеджер видит на какое именно сообщение клиент ответил

**Технические изменения:**
- Новая таблица `message_mapping` в database.py
- Функции: `save_message_mapping()`, `get_group_message_id()`, `get_client_message_id()`
- При отправке сообщения от клиента -> сохраняется маппинг
- При ответе менеджера -> сохраняется маппинг
- При цитате -> reply на соответствующее сообщение в группе

### Исправлен EditForumTopic

- В Pyrofork API изменился: `messages.EditForumTopic` с параметром `peer=` (вместо `channels.EditForumTopic` с `channel=`)
- Добавлена обработка ошибки `TOPIC_NOT_MODIFIED`

### GitHub

- Репозиторий: https://github.com/dscfrd/cfrd-support-bot-v2
- Авторизация: `gh auth login`
- Push: `git push origin master`

### Reply менеджера клиенту (NEW!)

Когда менеджер отвечает на сообщение в треде с командой, клиент получает ответ как reply:
```
Reply на сообщение клиента + /4 текст ответа
→ Клиент видит ответ как reply на своё сообщение
```

### Уведомления о редактировании/удалении (NEW!)

**Клиент отредактировал сообщение:**
```
✏️ Имя @username изменил на:
новый текст
```
(приходит как reply на оригинальное сообщение)

**Клиент удалил сообщение:**
```
🗑 Имя @username удалил сообщение
```
(приходит как reply на оригинальное сообщение)

### Команда /del (NEW!)

Менеджер может удалить своё сообщение у клиента:
1. Reply на своё сообщение в треде
2. Написать `/del`
3. Сообщение удаляется у клиента

### Технические изменения (продолжение)

- Добавлено поле `message_text` в таблицу `message_mapping` для хранения текста
- Функция `update_message_text()` для обновления текста в маппинге
- Обработчик `on_edited_message` для отслеживания редактирования
- Обработчик `on_deleted_messages` для отслеживания удаления
- Команда `/del` для удаления сообщения у клиента

### Исправленные баги

- **Баг Pyrofork с send_media_group**: `Messages.__init__() missing 'topics'`
  - Обёрнуто в try/except, ошибка игнорируется (сообщения всё равно отправляются)

---

## ГОТОВО (обновлено)

Добавлено:
- ✅ Оповещение об удаленном сообщении
- ✅ Оповещение об отредактированном сообщении
- ✅ Удалить сообщение (менеджер может удалить своё сообщение у клиента)
- ✅ Reply менеджера отображается как reply у клиента
- ✅ Оповещение о реакции клиента (добавление/удаление)

### Оповещение о реакциях (NEW!)

Когда клиент ставит/убирает реакцию на сообщение:
```
👍 Имя @username поставил реакцию: ❤️ 👍 🔥
➖ Имя @username убрал реакцию
```

- Если сообщение в маппинге - уведомление как reply
- Кастомные эмодзи (premium) отображаются как ✨

---

---

## Изменения сессии 2025-12-14

### Команда /company (NEW!)

Установка названия компании для клиента:
```
/company 4 ООО Ромашка
```
- Заголовок треда: `4: Имя Клиента | ООО Ромашка`
- Добавлено поле `company_name` в таблицу `clients`

### Новый формат сообщений от клиента

Убран @username, добавлена стрелка-ссылка на профиль:
```
**Имя Фамилия ↗**

Текст сообщения
+ 1 видео
```
Где `↗` — ссылка на профиль клиента (tg://user?id=...)

### Обновлён deprecated API

- `message.forward_from` → `message.forward_origin`
- Создана функция `get_forward_info()` для работы с пересылками
- Убраны warnings в логах

### Reactions Bot (reactions_bot.py)

Создан отдельный бот для отслеживания реакций:
- Токен: business bot @cfrdmega_bot
- **ОГРАНИЧЕНИЕ:** Telegram Business API не передаёт реакции клиентов через Bot API
- Бот готов к использованию когда Telegram добавит поддержку

### Технические изменения

- `database.py`: добавлены функции `set_company_name()`, `get_company_name()`, `get_client_info_for_thread_title()`
- `bot.py`: добавлена команда `/company`, обновлён формат user_name во всех местах
- `/help` обновлён с новой командой

---

### Система управления менеджерами (NEW!)

**Тиры клиентов:**
- `/tier [ID_треда] [1|2|3]` - установить тир клиента (tier-1, tier-2, tier-3)
- Добавлено поле `tier` в таблицу `clients`

**Отпуск/больничный:**
- `/vacation @менеджер @заместитель [tier-1 tier-2 tier-3]` - передать треды заместителю
- `/return @менеджер` - вернуть треды из отпуска
- Можно указать конкретные тиры для частичной передачи
- Новая таблица `manager_substitutions` для отслеживания замещений

**Увольнение:**
- `/team` - показать карточки всех менеджеров (имя, ID, username, тредов, статус)
- `/fire @новый_менеджер` (reply на карточку) - уволить менеджера
  - Передать все треды новому менеджеру
  - Удалить из базы данных
  - Удалить из группы

**Технические изменения:**
- `database.py`: функции `set_client_tier()`, `get_client_tier()`, `get_threads_by_tier()`
- `database.py`: функции `start_vacation()`, `end_vacation()`, `is_on_vacation()`, `get_vacation_info()`
- `bot.py`: команды `/tier`, `/vacation`, `/return`, `/team`, `/fire`
- Удалены старые команды `/reassign`, `/unassign`, `/remove_manager`

---

## Изменения сессии 2025-12-14 (продолжение)

### Новый формат заголовков тредов

Если у клиента задан custom_id (через /id), он используется вместо номера треда:

**Если ID задан:**
```
Иванов: Иван Петров | ООО Ромашка (123)
```

**Если ID не задан:**
```
123: Иван Петров | ООО Ромашка
```

**Технические изменения:**
- `database.py`: новая функция `format_thread_title()` для единообразного формата
- `get_client_info_for_thread_title()` теперь возвращает `custom_id`
- Обновлены: `mark_thread_urgent()`, `/company`, `/id`
- Заголовок автоматически обновляется при установке /id или /company

### Команда /readme (NEW!)

Скачать полное руководство по боту:
```
/readme
```
- Отправляет файл README.txt
- Добавлена в /help

**Содержание README.txt:**
- Принцип работы бота (схема клиент → бот → группа)
- Установка (требования, пошаговая инструкция)
- Все команды с примерами
- Формат заголовков тредов
- Настройки уведомлений
- Структура проекта
- Описание таблиц базы данных
- UTF-8 BOM для корректного отображения в Windows

---

## ГОТОВО (обновлено 2025-12-14)

- ✅ /company - название компании в треде
- ✅ Формат сообщений: `Имя ↗` (стрелка = ссылка на профиль)
- ✅ Deprecated forward_from API обновлён на forward_origin
- ✅ Критичные: пересланные файлы и видео работают
- ✅ /tier - тиры клиентов (tier-1, tier-2, tier-3)
- ✅ /vacation, /return - система замещения менеджеров
- ✅ /team, /fire - управление командой и увольнение
- ✅ Новый формат заголовков: custom_id вместо номера треда (номер в скобках)
- ✅ /readme - скачать полное руководство

---

## Изменения сессии 2025-12-14 (часть 2)

### Graceful Shutdown (NEW!)

Бот корректно завершает работу при получении сигналов SIGTERM/SIGINT:
- Ждёт завершения отправки медиа-групп (до 5 секунд)
- Логирует причину остановки
- Корректно закрывает сессию Pyrogram

**Технические изменения:**
- Добавлен `import signal`
- Глобальная переменная `shutdown_requested`
- Функция `graceful_shutdown(sig_name)`
- Обработчик сигналов `signal_handler()`

### Хранилище файлов и шаблоны (NEW!)

Канал-хранилище FILES_CHANNEL_ID = -1003563685324 (Архив_support)

**Команды:**
- `/upload` - загрузить файл (reply на файл с caption = имя)
  - Только для администраторов группы
- `/file [имя]` - отправить файл клиенту
- `/temp [имя]` - отправить файл + текст шаблона клиенту
- `/files` - список всех сохранённых файлов

**Workflow для /temp:**
1. Менеджер пишет `/temp визитка` в треде
2. Бот спрашивает "Введите текст для отправки вместе с файлом"
3. Менеджер вводит текст
4. Клиент получает файл + текст

**Технические изменения:**
- `config.py`: добавлен FILES_CHANNEL_ID
- `database.py`: таблица `file_templates`, функции `save_file_template()`, `get_file_template()`, `list_file_templates()`, `delete_file_template()`, `update_template_text()`
- `bot.py`: команды `/upload`, `/file`, `/files`, `/temp`, обработчик ввода текста шаблона

### Фото менеджеров в канале (NEW!)

Фото менеджеров хранятся в канале FILES_CHANNEL_ID чтобы не терялись после перезапуска:

**Как работает:**
1. При /auth менеджер отправляет фото
2. Фото сохраняется в канал с подписью "📸 Фото менеджера: Имя (ID: 123)"
3. message_id сохраняется в БД (колонка `photo_channel_message_id`)
4. При /card фото берётся из канала по message_id

**Технические изменения:**
- `database.py`: колонка `photo_channel_message_id` в `managers`
- `database.py`: функция `get_manager_photo_channel_id()`
- `bot.py`: обновлён `handle_manager_photo()` - сохраняет в канал
- `bot.py`: обновлён `send_manager_card_to_client()` - берёт из канала

---

## ГОТОВО (обновлено 2025-12-14)

- ✅ Graceful shutdown (SIGTERM/SIGINT)
- ✅ /upload - загрузка файлов в хранилище
- ✅ /file - отправка файла клиенту
- ✅ /temp - отправка файла с текстом
- ✅ /files - список файлов
- ✅ Фото менеджеров в канале (не теряются после перезапуска)
- ✅ Убран добавочный номер из /auth и подписи менеджера
- ✅ Кастом эмодзи менеджеров (фото в подписи вместо обычного эмодзи)

### Убран добавочный номер (extension)

**Было:**
- `/auth 🔧, Иван Петров, Технический специалист, 1234`
- Подпись: `🔧 Иван Петров, Технический специалист, доб. 1234`
- Карточка: "наберите добавочный: 1234"

**Стало:**
- `/auth 🔧, Иван Петров, Технический специалист`
- Подпись: `🔧 Иван Петров, Технический специалист`
- Карточка: без добавочного

**Изменения:**
- `database.py`: `save_manager()` - extension теперь опциональный (default="")
- `bot.py`: `/auth` принимает 3 части вместо 4
- `bot.py`: подпись без ", доб. {extension}"
- `bot.py`: карточка без строки с добавочным
- `README.txt`, `/help` - обновлена документация

---

## ПЛАНЫ НА БУДУЩЕЕ

### WebRTC бот для голосовых звонков (ОПЦИОНАЛЬНО)

**Отдельный бот** для голосового общения с клиентами:
- WebRTC для звонков прямо из Telegram
- Интеграция с **Whisper OpenAI** - расшифровка аудио в текст
- Интеграция с **Grok** - резюмирование разговоров
- Транскрипты и саммари сохраняются в тред

**ВАЖНО:** Это дополнительный бот, не входит в основную структуру. Основной бот работает полностью автономно без него.

### Кастомные эмодзи менеджеров — РЕАЛИЗОВАНО ✅

**Как работает:**
1. При `/auth` менеджер загружает фото
2. Фото автоматически обрезается в круг 100x100 px
3. Добавляется в стикерпак `cfrd_managers_emoji`
4. `custom_emoji_id` сохраняется в БД
5. В подписи сообщений используется кастом эмодзи вместо обычного

**Рекомендация для фото:** лицо должно занимать ~80% кадра

**Технические детали:**
- Стикерпак: `t.me/addemoji/cfrd_managers_emoji`
- Конфиг: `EMOJI_PACK_NAME` в config.py
- Функция: `add_manager_emoji()` — создаёт эмодзи из фото
- Функция: `format_signature_with_custom_emoji()` — форматирует подпись с entities
- Поле в БД: `custom_emoji_id` в таблице `managers`
- Отправка через `entities` вместо `parse_mode` для кастом эмодзи

---

---

## Изменения сессии 2025-12-15

### Исправлены команды /team и /help (BUG FIX!)

**Проблема:**
Команды `/team` и `/help` не работали когда их отправлял владелец бизнес-аккаунта (через который работает бот).

**Причина:**
Баг в pyrofork — фильтры `filters.command()` и `filters.regex()` не срабатывают для "исходящих" сообщений (`outgoing=true`). Когда владелец аккаунта отправляет сообщение в группу, оно помечается как исходящее и фильтры его игнорируют.

**Решение:**
Создан **роутер команд** (`command_router`) в начале обработчиков:

```python
@business.on_message(filters.chat(SUPPORT_GROUP_ID), group=-10)
async def command_router(client, message):
    # Обрабатываем только исходящие сообщения
    if not message.outgoing:
        return

    # Ручная маршрутизация команд
    if text.startswith('/team'):
        await _handle_team_command(client, message)
    elif text.startswith('/help'):
        await _handle_help_command(client, message)
```

**Как работает:**
1. Роутер ловит ВСЕ сообщения из группы поддержки (group=-10, самый высокий приоритет)
2. Проверяет `message.outgoing` — если False, пропускает (для других менеджеров)
3. Для исходящих сообщений вручную направляет команды на внутренние функции
4. Входящие сообщения от ДРУГИХ менеджеров обрабатываются стандартными хэндлерами с `filters.command()`

**Технические изменения:**
- `bot.py`: добавлен `command_router()` в начало файла
- `bot.py`: добавлены `_handle_team_command()` и `_handle_help_command()` — реализации для роутера
- Стандартные хэндлеры `/team` и `/help` оставлены для входящих сообщений от других менеджеров

---

## ГОТОВО (обновлено 2025-12-15)

- ✅ Исправлены команды /team и /help для владельца бизнес-аккаунта

---

## Как продолжить работу

Скажи Claude:
```
продолжим с support bot v2
```
